require 'spec_helper'
Dir["./lib/*.rb"].each { |file| require file }

describe FunnyPrinter do
  let(:team) { Team.new }

  it "FunnyPrinter prints members names" do
    pending

    raise "No code - no honey"
  end
end